<?xml version="1.0" encoding="utf-8"?>
<!-- Template for the Microsoft Store MSIX package. Placeholders are filled
     by scripts/build-msix.ps1 from Partner Center's Product Identity page:
     {{IDENTITY_NAME}}      Package/Identity/Name  (e.g. 12345ShortArrow.Menreiki)
     {{PUBLISHER}}          Package/Identity/Publisher (CN=xxxx-guid)
     {{PUBLISHER_DISPLAY}}  Publisher display name
     {{VERSION}}            four-part version (e.g. 0.1.0.0) -->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities">
  <Identity Name="{{IDENTITY_NAME}}"
            Publisher="{{PUBLISHER}}"
            Version="{{VERSION}}"
            ProcessorArchitecture="x64" />
  <Properties>
    <DisplayName>Menreiki</DisplayName>
    <PublisherDisplayName>{{PUBLISHER_DISPLAY}}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>
  <Resources>
    <!-- The UI is Japanese-only (ADR-011); declaring more would be untrue
         and would force store listings for languages the app doesn't have. -->
    <Resource Language="ja-JP" />
  </Resources>
  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop"
                        MinVersion="10.0.17763.0"
                        MaxVersionTested="10.0.26100.0" />
  </Dependencies>
  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
  <Applications>
    <Application Id="Menreiki"
                 Executable="menreiki-desktop.exe"
                 EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="Menreiki"
        Description="Local-first document de-identification, pseudonymization and generalization workbench."
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png" />
      <Extensions>
        <uap:Extension Category="windows.fileTypeAssociation">
          <uap:FileTypeAssociation Name="mnrk">
            <uap:SupportedFileTypes>
              <uap:FileType>.mnrk</uap:FileType>
            </uap:SupportedFileTypes>
          </uap:FileTypeAssociation>
        </uap:Extension>
      </Extensions>
    </Application>
  </Applications>
</Package>
