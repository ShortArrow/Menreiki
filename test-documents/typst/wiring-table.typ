// ユースケース: 罫線表（電源系統・結線表）
//
// 「MAIN+28」「GND」のようなセルが縦に繰り返される表では、
//   - 縦に並ぶ「+28」列が連結されて「+28+28…」→国際電話と誤検出
//   - 縦罫線で区切られた隣接セルが1語に結合
// という回帰を踏んだ。検証ポイント:
//   - phone 候補が捏造されないこと（格子は結合しない）
//   - 隣接セル（数量/単価など）が別語のままであること
#set page(paper: "a4", margin: (top: 2.5cm, bottom: 2.5cm, x: 2cm))
#set text(font: ("Yu Gothic", "MS Gothic"), size: 10.5pt, lang: "ja")

#align(center)[#text(size: 14pt, weight: "bold")[電源系統 結線表]]
#align(center)[装置: RX-77 信号変換器（S/N: SN-24070912）]

#v(0.6cm)

#table(
  columns: 4,
  [端子], [系統], [リターン], [備考],
  [TB1-1], [MAIN+28], [GND], [幹線],
  [TB1-2], [MAIN+28], [GND], [],
  [TB1-3], [MAIN+28], [GND], [],
  [TB1-4], [MAIN+28], [GND], [],
  [TB1-5], [MAIN+28], [GND], [],
  [TB1-6], [MAIN+28], [GND], [],
  [TB1-7], [MAIN+28], [GND], [予備],
  [TB1-8], [MAIN+28], [GND], [予備],
)

#v(0.6cm)

= 部品表
#table(
  columns: 4,
  [品名], [型式], [数量], [単価],
  [コネクタ], [CN-D9F-2026], [8], [¥420],
  [電線], [AWG20-BK], [12 m], [¥85],
)

問い合わせ先: 猫埼電工株式会社 資材部（TEL 045-111-2222）
